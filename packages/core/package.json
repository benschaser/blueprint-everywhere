{
  "name": "@blueprint-everywhere/core",
  "version": "0.1.0",
  "description": "Core design tokens for the Blueprint Everywhere design system.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "ts-node scripts/index.ts",
    "clean": "rimraf dist",
    "lint": "eslint scripts --ext .ts",
    "verify": "ts-node scripts/verify-tokens.ts"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "ts-node": "^10.9.2",
    "rimraf": "^6.0.0"
  }
}
